using static System.Math;

public static class math {
	static void Main(){
		double sqrt2=Sqrt(2.0);
		System.Console.Write($"sqrt(2) = {sqrt2});
		System.Console.Write($"sqrt2*sqrt2 = {sqrt2*sqrt2}\n};
	

		double exppi=Exp(PI);
		System.Console.Write.($"exp(pi) = {exppi}\n");

		double pi=Pow(PI,E); System.Console.Write($"pi to the power of e = {pi}\n");
	}
}
